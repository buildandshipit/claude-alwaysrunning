/**
 * Triggers Module - Exports
 */

const { TriggerService, getTriggerService, resetTriggerService } = require('./trigger-service');

module.exports = {
  TriggerService,
  getTriggerService,
  resetTriggerService,
};
